{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "pulp build && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "rimraf": "^2.5.4"
  },
  "dependencies": {
    "incremental-dom": "^0.5.1"
  }
}
