{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "pulp build && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "rimraf": "^2.5.4"
  },
  "dependencies": {
    "incremental-dom": "^0.4.1"
  }
}
